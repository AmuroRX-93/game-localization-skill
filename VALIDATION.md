# v0.5.0 校对稿与项目经验更新

日期：2026-09-26。

- 使用用户提供的参考工作簿生成 3 个任务、29 条内容；独立回读核对全部可见值、有效合并范围、宋体 11、表头配色、数值报酬和首行冻结。完整任务块预览已检查，长文本行高按换行扩展。
- 验证重复显示编号保留、技术 UID 唯一、缺失日文不捏造；已有输出和重复 UID 输入被拒绝。原工作簿未修改，参考正文及生成的实数测试文件不随发行包公开。
- 现有 40 项 unittest、合成资源 CLI 自测和 Skill 结构检查通过。发行包沿用同源构建；包内文件与源码一致，重复构建校验一致。
- XLSX 脚本已在本机 macOS 的 Node.js / `@oai/artifact-tool` 环境测试；未在 Windows、其他客户端或真实 Excel 中运行。此可选依赖和 Node 运行时不随包分发。
- 脚本仅导出工作簿和映射，不提供任意重排后的自动导入。新增的字体/贴图/PS3 文档是项目经验与验证要求，不代表本版新增了所有格式适配器或完成 PS3 实机测试。

---

# v0.4.0 工具包验证记录

日期：2026-09-25。以下是本版实际执行结果，旧版记录保留在后面。

## 已执行

- 合计 40 项 unittest 通过：19 项原清单检查器测试、21 项新增格式/差分/贴图测试。包括未知格式、边界、重复 ID、非零填充、token、错误基线、损坏差分、已有输出拒绝和临时文件清理。
- 虚构资源 CLI 自测走通 XMB 抽取、翻译回填、字形清单检查、ARB 变长替换、未改成员保留、差分重建和 SHA 一致；覆盖中文含空格路径。
- 原始工程的 420 份 XMB 通过便携读写器原样往返。真实 pause2 FPK 的 121 个成员、TEX 的 40 个成员可枚举；实际 GTF 中文候选构建、9 级 mip 与预览解码成功，大小/头部与未选压缩块保持。真实资源和所用系统字体不随包公开。
- 合成 BC3 / 线性 ARGB 样本原样往返；使用自行生成的几何字形 TTF 验证改字和未选块/像素保留。几何字形不是中文字体质量证据。
- 五份最终解压包在不同目录执行 CLI 自测均通过，包内相对引用与私人路径检查通过；六个 ZIP 重复构建 SHA 一致。
- Skill frontmatter/名称/占位模板检查通过。发行包来自同一份技能目录，无系统路径或写死工程根目录。

## 独立 agent 前向验收

另一 agent 只得到技能位置、限定的临时输出权限和一个汉化小样任务，未提供预期译文或实现结论。它按包内说明独立完成：

- 5 条日英混合句子转简中，保留 `[$name]`、`%d`、`{power}`、`{armor}`、换行和空串，重新抽取一致。
- 新 ARB 封回、未修改成员保持、容器差分创建和重建。新容器与差分还原 SHA 一致。
- token 丢失、伪 XMB、错误成员 SHA、覆盖源输出被拒绝，输入保持原字节。
- 自行生成字体和 128×64 ARGB 合成纹理，完成改字及预览，验证头部、长度和框外像素保持。

未发现阻断独立调用的缺陷；顶层帮助简略，需要配合 references/toolkit.md 阅读。这个结果验证的是该 agent 的小样使用能力，不是所有模型或客户端认证。

## 仍需按项目验证

- 没有用本便携工具的新候选进入真实游戏验收；不声称已解决所有游戏的汉化问题。
- 未执行 Windows/Linux 或 Cursor、Kimi、Qwen、CodeBuddy 客户端内测试。五份是安装目录适配和同源脚本分发；运行还需要 Python 与相应权限。
- 不覆盖未列出的 XMB/ARB/FPK/TEX/GTF 变体、PS2/FromSoftware 专用封包或 SDAT 认证加解密。
- font-check 只检查 cmap；文字语义、字形外观、动态裁切、动画、内存、加载缓存和长时间运行仍须人工/agent 按场景验证。

---

# v0.3.0 验证记录

日期：2026-09-20。

## 已执行

- Skill frontmatter、名称和未完成模板检查通过。
- 清单检查器的 19 项自动测试通过，包含条目遗漏、重复身份、未知资源、token 丢失／顺序／重复次数、过期构建测试、空证据、部分抽取及错误类型。
- CLI 以中文含空格文件路径运行；验证 0／1／2 退出码、JSON 输出及输入文件不被修改。
- 虚构示例能显示待办，不误报闭环完成。
- 本地 Markdown 引用、分发文件列表和私人信息模式检查已执行。

## v0.3.0 增量验证

- 新增 Kimi Code、Qwen Code、CodeBuddy 包和安装说明；五种工具共用同一份技能核心内容。
- 总包含五个独立工具目录及总说明；逐文件验证与五个单独包相同，无重复归档路径。
- 在中文含空格路径解压总包，五份检查器均可运行，示例均按预期返回未完成；技能引用完整，内容逐字节与源码一致。
- 六个压缩包的重复构建 SHA-256 一致。现有 19 项检查器测试再次通过。
- 不新增国产客户端或 Cursor 内实测的声明；适配为目录、入口和文档层面。

## v0.2.0 增量验证

- 明确日译中、英译中与混合资源范围；校对表和术语表不再默认日文原文。
- 增补短词多义、否定与条件、复数分支、源文版本不一致等语言审校场景，属于文档审查，未证明模型翻译质量。
- 通用包与 Cursor 包由同一目录生成；解包逐文件比对，Cursor 仅省略 Codex 专用的 `agents/openai.yaml`。
- 在中文含空格路径解压 Cursor 包，执行其中的检查器并确认示例按预期返回未完成状态；检查包内相对链接。
- 对照 Cursor、Kimi Code、Qwen Code、CodeBuddy 官方文档核对技能目录；尚未在这四个客户端实际验证发现／执行过程。

## 流程场景审查

以下为作者按文档进行的桌面审查，不是另一个模型的独立评估，也不是实际运行这些游戏的新实验。

| 输入场景 | 检查的预期行为 | 对应说明 |
|---|---|---|
| 只要五段游戏内原译文做表格 | 交付可编辑双语表，不改游戏镜像 | SKILL、文本与覆盖率 |
| 一页几百行，声称全量数千条 | 分开工作表／段落／物理出现次数 | 文本与覆盖率、清单工具 |
| 同系列字库能否直接移植 | 检查编码、字形、消费者和资源结构 | 资源写回、适配器 |
| 全部换高清字体 | 先测代表界面与预算，不盲增图集 | 资源写回与显示 |
| 原文已译但按键帮助空白 | 查终止符、绑定、加载，不只补译 | 资源写回与显示 |
| 视频电脑能播却游戏黑屏 | 对照原片，测跳过和战斗转场 | 影音与字幕 |
| 对比 PSP 术语 | 复用确认词汇，保留平台特有语义 | 文本与覆盖率 |
| 用户只想研究手操功能 | 不安装／发布，也不混入汉化 | SKILL |
| 现代引擎有官方翻译接口 | 先用原生语言资源，不强制 ROM 逆向 | 适配器 |
| 字体历史余量约 69 KiB | 不保证最坏场景；去重与保护分开 | 资源写回、案例 |
| 无任何资源或运行测试 | 清单保留缺口，不能宣称全游戏完成 | 清单工具 |
| 发布请求已经授权 | 完成包与检查后直接发布并读回远端 | 验证与交付 |

## 尚未证明

- 没有为所有主机、引擎、编码或游戏实现解析器。
- 本次未重新完整游玩历史项目，也未证明历史版本所有问题已修复。
- 没有对新游戏执行端到端汉化试验；未来应逐项目积累适配器与回归样本。
- 清单工具检查自述数据的内部一致性，不验证证据真实性、语言质量、资源分母是否完整。
- 文档经过来源对照与作者审查，尚未经过外部汉化团队的独立审阅。
